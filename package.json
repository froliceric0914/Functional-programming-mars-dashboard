{
  "name": "jsnd3_mars_dashboard",
  "version": "1.0.0",
  "description": "course project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && nodemon src/server/index.js localhost 3000",
    "watch": "watchify src/public/client.js -o src/public/bundle.js -v",
    "build": "browserify src/public/client.js -o src/public/bundle.js "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "immutable": "^4.0.0-rc.12",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "nodemon": "^2.0.7",
    "watchify": "^4.0.0"
  }
}
